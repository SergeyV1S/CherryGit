# Доработки до полной готовности CherryGit

Документ составлен на основе функциональных требований ВКР и фактического состояния кодовой базы на 2026-05-22.

## Шкала готовности

- 🟢 **80–100%** — работает, нужны минорные правки
- 🟡 **20–70%** — скелет/частично реализовано
- 🔴 **0–15%** — не реализовано

---

## 1. Сбор данных из GitLab

### 1.1. GitLab API клиент (FR-01) — 🟢 100%

Готово полностью: PAT auth, пагинация, timeout, retry на 429, маппинг ошибок, шифрование токенов, audit. Доработок нет.

### 1.2. Подключение проектов к командам (UC-01 шаги 6–9) — 🟢 95%

**Реализовано:**

- `connectProject(actorUid, dto)` — UC-01 шаги 6–10 целиком: проверка connection, валидация teamUids, fetch метаданных проекта с GitLab, транзакционный insert в `projects` + `team_projects` + `sync_statuses`
- `updateProject(uid, dto, actorUid)` — патч `releaseTagPattern`/`hotfixLabel`/`revertLabel` + полная замена связей с командами
- `deleteProject(actorUid, uid)` — снятие связей и `sync_statuses`, исторические commits/MR намеренно сохраняются
- `triggerResync(actorUid, uid)` — форсированный пересбор (фактически делегирует в Sync, который ещё `notImplemented`)
- `listProjects()` с агрегацией команд через JOIN
- `getProject(uid)` возвращает `{project, teams, syncStatus}`
- Защита от дубликата через PG unique constraint `uq_project_per_connection`, маппинг `23505` → HTTP 409 (с проверкой и `error.cause.code` на случай оборачивания Drizzle)
- Zod-валидация DTO в контроллере (`connectProjectSchema`, `updateProjectSchema`)
- Audit: `project.connected` / `project.updated` / `project.disconnected` / `project.resync_triggered`
- Endpoint `POST /admin/projects/:uid/resync` добавлен в router
- Fire-and-forget вызов `SyncService.syncProject` при подключении — заработает автоматически когда 1.3 будет готов

**Дополнительно реализовано (после интеграции с 1.3):**

- **Колонка `projects.defaultBranch`** добавлена в схему; `connectProject` сохраняет `remote.default_branch` из GitLab — это разблокировало корректный `fetchCommits(ref=defaultBranch)` в sync-пайплайне.
- **Бутстрап-sync** и `triggerResync` работают — делегируют в `SyncService.syncProject`, который теперь реализован.

**Осталось доработать:**

- **Миграция БД**: после `git pull` нужно `yarn generate && yarn migrate` для применения новых столбцов (`projects.default_branch`).
- **Валидация `releaseTagPattern` как glob** — сейчас принимается любая непустая строка. Полная проверка glob нужна только при матчинге в 1.4; самописный матчер (`glob-match.ts`) терпим к мусору (вернёт false).
- **Авторизация владельца GitLab-подключения**: сейчас любой ADMIN может подключить проект к любому connection (формально соответствует ВКР 2.2.7, но если будет «администратор отдела» — потребуется проверка `connection.ownerUid === actorUid OR role === ADMIN`).
- **Пагинация `listProjects`** — для MVP не нужна (десятки проектов), но при росте — добавить limit/offset.

### 1.3. Периодический инкрементальный sync (FR-02) — 🟢 80%

**Реализовано:**

- **Пайплайн** `syncProject(actorUid, projectUid)`:
  1. `fetchCommits(since=lastSyncAt, ref=defaultBranch)` → upsert `commits` через `ON CONFLICT (project_uid, sha)`
  2. `fetchMergeRequests(updated_after=lastSyncAt)` → upsert `merge_requests` с фазами Cycle Time (firstReviewAt, approvedAt, mergedAt, closedAt) и MR Size (linesAdded/Removed/filesChangedCount)
  3. Для каждого MR параллельно через `Promise.all`: `fetchMergeRequestNotes` + `fetchMergeRequestApprovals` + `fetchMergeRequestChanges` (notes переиспользуются между MR-апсёртом и `upsertReviews`, без двойного GitLab-запроса)
  4. `fetchMergeRequestCommits` → связывание через `mr_commits` (`ON CONFLICT DO NOTHING`)
  5. `fetchTags` → классификация по `releaseTagPattern` через самописный glob-матчер → upsert `deployments` через `ON CONFLICT (project_uid, tag)`
- **Маркеры hotfix/revert**: `hasHotfixLabel`/`hasRevertLabel` заполняются проверкой пересечения `MR.labels` с массивами `project.hotfixLabels`/`project.revertLabels` (доработка 1.4) — готово для CFR (2.5)
- **Мягкий лок**: `sync_statuses.status='syncing'` ставится в начале, проверка на входе → HTTP 409 при попытке параллельного запуска
- **Идемпотентность**: все `ON CONFLICT DO UPDATE`; парциальный прогресс сохраняется — при ошибке `lastSyncAt` НЕ обновляется, следующий tick повторит интервал
- **Audit**: `sync.completed` (с метриками) и `sync.failed` (с partialReport)
- **Планировщик** (`sync.scheduler.ts`): `setInterval` с защитой от перекрытия tick'ов (флаг `isRunning`), `runOnStart` опция, `stopScheduler()` для graceful shutdown
- **Конфигурация**: `SYNC_INTERVAL_M` и `SYNC_RUN_ON_START` env, default 10 минут
- **Bootstrap**: `startScheduler` вызывается после `app.listen` в `main.ts`
- **`syncAllProjects`**: graceful fail, ошибка одного проекта не останавливает обход остальных. Передаёт `actorUid=undefined` (system event) — корректно для FK на `users.uid` и nullable `auditLogs.userUid`
- **Glob-матчер** (`glob-match.ts`): поддержка `*`, `**`, `?`, `[...]` — покрывает реальные паттерны (`v*`, `release-*`, `v[0-9]*.*`)
- **Endpoint manual trigger**: `POST /admin/sync/projects/:projectUid/run` и `POST /admin/projects/:uid/resync` оба работают
- **Endpoint статуса**: `GET /admin/sync/projects/:projectUid/status` + `getProject` (projects module) возвращает syncStatus

**Осталось доработать:**

- **Маппинг GitLab user → CherryGit user для commits** (см. также 4.4):
  - GitLab `/repository/commits` НЕ возвращает username — только `author_name` и `author_email`. Сейчас в `commits.authorGitlabUsername` пишется `author_email`, `authorUid = null`.
  - Для резолва нужно расширить `user_gitlab_identities` колонкой `email` (опционально, с unique по `(connectionUid, email)`) и добавить вторую попытку резолва по email.
  - Без этого commit-метрики (Bus Factor по авторам) не привяжутся к CherryGit-юзерам, только к email-строкам.
- ~~**Связь `deployment_merge_requests` не заполняется**~~ — ✅ реализовано в доработке 1.4: `linkDeploymentsToMergeRequests` в `sync.service.ts` для каждого деплоя берёт merged MRs в окне `(prevDeploy.deployedAt, currentDeploy.deployedAt]` с `target_branch === project.defaultBranch` и вставляет в `deployment_merge_requests` (ON CONFLICT DO NOTHING). На основе связанных MR поднимаются `deployments.isHotfix`/`isRevert`.
- **Staging-таблица `gitlab_raw_payloads` не заполняется** (ВКР 2.2.5): архитектурно предусмотрен буфер «raw → processed», сейчас пайплайн парсит и пишет в нормализованные таблицы в одной фазе. Для MVP допустимо, но: (а) сложнее отлаживать расхождения с GitLab, (б) при ошибке парсера нужно повторно дёргать GitLab, а не переигрывать payload. Решение: либо реализовать staging, либо отметить в тексте ВКР «упрощённая реализация без буфера».
- **Batching для первого sync** проекта с большой историей: сейчас `fetchMergeRequests` без `since` загружает ВСЕ MR в память + 4 GitLab-запроса per MR. Для проекта с 10k MR это часы и сотни МБ RAM. Доработка: чанкование по N MR (100–200), промежуточные commit'ы в БД и `lastSyncAt`-bookmark между батчами.
- **Параллелизация по разным connections** в `syncAllProjects`: сейчас все проекты обходятся последовательно. Безопасная оптимизация — параллелить только проекты с РАЗНЫХ GitLab-инстансов (rate limit per-instance), внутри одного instance — sequential.
- **Чанкование больших `insert` batch'ей** для commits — Postgres `bind message length` падает на >65k параметров; нужно резать на батчи по ~1000 строк.
- **Очистка расшифрованного PAT-токена из памяти** после sync — сейчас живёт всё время прогона (до часов). Минор по безопасности.
- **Метрика `commitsUpserted`** считается как длина пачки, не реально вставленных — название вводит в заблуждение, можно переименовать в `commitsFetched`.
- **`recalculateMetrics`** в `SyncService` всё ещё 501 — это доработка 2.7 (snapshot writer).

### 1.4. Классификация деплоев и инцидентов (FR-03) — 🟢 90%

**Реализовано:**

- **Glob-матчер `releaseTagPattern`** (`v*`, `release-*`, `v[0-9]*.*`) — самописный `glob-match.ts`, покрывает реальные паттерны MVP; `minimatch` не подключали ради минимальных зависимостей.
- **Маппинг tag → `deployments`** в `upsertDeploymentsFromTags`: `deployedAt = tag.commit.committed_date`, `tag`, `commitSha`. Идемпотентность через `ON CONFLICT (project_uid, tag) DO UPDATE`. На каждом upsert `isHotfix/isRevert` сбрасываются в `false` (затем поднимаются линкером — корректно отрабатывает удаление меток из проекта).
- **Множественные метки инцидентов**: `projects.hotfixLabel` (text) → `hotfixLabels` (text[], default `{hotfix, rollback}`); `revertLabel` → `revertLabels` (default `{revert}`). MR классифицируется как hotfix/revert если ХОТЯ БЫ одна его метка пересекается с соответствующим массивом проекта.
- **Связь deploy ↔ MR**: `linkDeploymentsToMergeRequests(projectUid, defaultBranch)` после `upsertDeploymentsFromTags`. Для каждого деплоя берутся merged MRs с `target_branch === defaultBranch` в окне `(prevDeploy.deployedAt, currentDeploy.deployedAt]`; для первого деплоя окно открыто слева. Bulk insert в `deployment_merge_requests` (ON CONFLICT DO NOTHING) — нужно для Lead Time (2.3).
- **Классификация инцидентов на уровне деплоя**: `deployments.isHotfix`/`isRevert` поднимаются, если хотя бы один связанный MR помечен соответствующим флагом.
- **Endpoint администратора**: `PATCH /admin/projects/:uid/hotfix-labels` с DTO `{ hotfixLabels?: string[], revertLabels?: string[] }` (хотя бы одно поле обязательно, дедупликация на стороне zod). Аудит `project.incident_labels_updated` с before/after-срезом. Защита от race condition между SELECT и UPDATE (404 при удалении проекта в момент апдейта).
- **Метрика прогресса**: `SyncReport.deploymentMrLinks` показывает суммарный размер связей за прогон.

**Известные ограничения / нюансы реализации (для текста ВКР):**

- **CFR-семантика — упрощённая** (важно для защиты!). DORA-каноническая интерпретация CFR: «доля деплоев, которые СЛОМАЛИ продакшен» — отметка ставится на _broken_ deploy, если в течение ~7 дней после него выходит hotfix-MR. У нас MVP-упрощение: помечается _fix_-deploy (тот, что содержит hotfix-MR). Числитель/знаменатель отношения идентичны (1 fix = 1 broken в подавляющем большинстве случаев), но семантика атрибуции отличается. В тексте ВКР — оговорить как намеренное упрощение, чтобы не пытаться сопоставить hotfix с broken через анализ message/issue (это требует системы инцидент-менеджмента, см. CLAUDE.md «За пределами MVP»).
- **Окно первого деплоя — открытое слева**: для самого первого `deployment` в проекте окном берётся `(-∞, deployedAt]`, что цепляет ВСЕ исторические merged MR. На свежеподключённом проекте с многолетней историей это даст один деплой с тысячами привязанных MR. Lead Time для него будет аномально большим, но это «технический» первый замер — на стационарном режиме искажение не накапливается.
- **Stale-флаги после `PATCH /hotfix-labels`**: `merge_requests.hasHotfixLabel`/`hasRevertLabel` и `deployments.isHotfix`/`isRevert` НЕ пересчитываются автоматически после смены меток — `MR.labels` GitLab у нас в БД не хранится (только производные boolean'ы). Чтобы переклассифицировать историю — админ обязан вызвать `POST /admin/projects/:uid/resync`. То же касается общего `PATCH /admin/projects/:uid` с полем `hotfixLabels` в теле. Стоит отразить в onboarding-доке админа.
- **N+1 в `linkDeploymentsToMergeRequests`**: 1 SELECT deployments + N SELECT'ов merge_requests + до N UPDATE'ов. Для 100 деплоев на проект и интервала sync = 10 мин это ~15k SQL-запросов/день/проект — допустимо для MVP, но при росте до 1000+ деплоев нужно оптимизировать (обрабатывать только новые деплои + последний; либо одним запросом с window-функцией).

**Осталось доработать:**

- **Миграция БД** под смену типа колонок `hotfix_label text` → `hotfix_labels text[]` и аналогично revert — генерируется через `yarn generate`. ⚠ drizzle-kit 0.22 обычно для переименования + смены типа предлагает интерактивный prompt «is this rename?» и при подтверждении строит `DROP` + `ADD` (потеря данных). Поскольку команда не может быть запущена интерактивно через `yarn generate`, после генерации проверить файл SQL вручную и при необходимости заменить `ALTER TABLE ... DROP COLUMN hotfix_label; ALTER TABLE ... ADD COLUMN hotfix_labels text[] DEFAULT '{hotfix,rollback}' NOT NULL;` на одну операцию: `ALTER TABLE projects ALTER COLUMN hotfix_label TYPE text[] USING ARRAY[hotfix_label]; ALTER TABLE projects RENAME COLUMN hotfix_label TO hotfix_labels; ALTER TABLE projects ALTER COLUMN hotfix_labels SET DEFAULT '{hotfix,rollback}';` (и аналогично revert). На свежей БД без данных — пустая разница, drizzle-kit подойдёт «как есть».
- **`environment='production'`** в `deployments` явно не пишем — в MVP теги по `releaseTagPattern` ВСЕГДА означают prod (других окружений не обрабатываем). Если потом понадобится staging/canary — добавить колонку и второй паттерн.
- **`isFailed`** остаётся всегда `false` — требует интеграции с мониторингом, вне MVP (см. CLAUDE.md «За пределами MVP»).
- **Валидация непересечения `hotfixLabels` и `revertLabels`**: сейчас можно прислать `{hotfix}` в оба набора, и MR с этой меткой будет одновременно `hasHotfixLabel=true` и `hasRevertLabel=true`. Не критично (отображается одинаково в CFR), но можно добавить refine в DTO.

---

## 2. Метрики (FR-04 и далее)

### 2.1. Cycle Time MR с декомпозицией — 🟢 95%

**Реализовано:**

- **Калькулятор** (`cycle-time-mr.calculator.ts:CycleTimeMrCalculator.compute`): чистая функция `merged: CycleTimeMrInput[] → CycleTimeMrValue`, без зависимости от БД (удобно тестировать).
- **Формулы фаз** (ВКР FR-09):
  - `timeToFirstReview = firstReviewAt - gitlabCreatedAt`
  - `timeInReview = approvedAt - firstReviewAt`
  - `timeToMergeAfterApproval = mergedAt - approvedAt`
  - `totalCycle = mergedAt - gitlabCreatedAt`
- **Агрегаты**: медиана и p90 (R-7 / линейная интерполяция, как Excel `PERCENTILE` и GitLab Insights) на total + каждой фазе. Реализовано в `lib/statistics.ts` (`median`, `quantile`, `p90`) — переиспользуется для 2.2-2.6.
- **Драфт-фильтр**: `^\s*(draft|wip)\b\s*[:\-]` case-insensitive — покрывает «Draft: ...» (текущий GitLab), «WIP:» (легаси до GitLab 12.10) и обе вариации с дефисом. Отброшенные MR подсчитываются в `excludedDrafts` (для прозрачности расчёта в UI).
- **Учёт пустых значений**: если у MR нет `firstReviewAt` или `approvedAt`, соответствующая фаза НЕ участвует в выборке этой фазы (фаза = null, а не 0). Размер выборки на фазу возвращается отдельно: `sampleSizePerPhase`.
- **Сервис** `metrics.service.ts:getTeamCycleTimeMr`:
  - `assertTeamAccess(actor, team)` — 404 если команды нет, 403 если LEAD не лид этой команды (роль проверяется в `team_members`, а не только глобально), HEAD не из этого отдела, и т.п.;
  - выборка merged-MR по проектам команды с `mergedAt ∈ [periodStart, periodEnd]`;
  - окно фильтрации — по `mergedAt`, чтобы MR попадал в метрику ровно того периода, в котором закрылся (стабильность исторических замеров).
- **Endpoint** `GET /api/teams/:teamUid/cycle-time-mr` уже зарегистрирован с `requireRole('LEAD', 'ADMIN')` (HEAD/MANAGER не имеет — см. CLAUDE.md «LEAD: Cycle Time MR с декомпозицией»).
- **Тип возврата** `TeamCycleTimeMrReport`: `{ metricType, periodStart, periodEnd, teamUid, projectUids, value }` — `projectUids` отдаются в ответе для прозрачности (по каким проектам шла выборка).
- **Smoke-тест**: на 6 синтетических MR (4 валидных + 2 драфта) калькулятор отдаёт правильные `median/p90/sampleSize/excludedDrafts` (проверено вручную).

**Осталось доработать:**

- **Снепшот в `metrics_snapshots`**: сейчас расчёт идёт on-demand при каждом GET. Это нагрузит БД при сотнях MR/команд. Сохранение результата в `metrics_snapshots` (для текущего периода и фиксированных «прошлых месяцев») — это уже задача 2.7.
- **Unit-тесты на `statistics.ts` и `CycleTimeMrCalculator`**: вручную smoke-тестировал, формальный jest-тест — задача 8.3.
- **Альтернативное окно по `gitlabCreatedAt`** (для дашборда «открытые MR» — сколько они уже висят): не нужно для MVP, но потенциальное расширение.

### 2.2. MR Size — 🟢 95%

**Реализовано:**

- **Калькулятор** (`mr-size.calculator.ts:MrSizeCalculator.compute`): чистая функция `merged: MrSizeInput[] → MrSizeValue`, без обращения к БД (как 2.1 — удобно тестировать, легко переиспользовать).
- **Жёстко заданные бакеты** (ВКР FR-15, концепция CherryGit): `≤50`, `51-200`, `201-400`, `401-800`, `>800` — в виде `BUCKETS: ReadonlyArray<{label, upper}>` (static на классе). Порядок «маленькие → крупные» — естественный X-axis столбчатой диаграммы.
- **Метрика размера** `size(mr) = linesAdded + linesRemoved`. Поля заполняются на этапе sync (`sync.service.ts:425`, `GitlabClient.computeMrSize`), переиспользуются как есть.
- **Распределение** возвращается как `buckets: {label, count, percent}[]` — массив, а не `Record<bucket, count>`, потому что:
  1. порядок бакетов критичен для UI (диаграмма слева-направо);
  2. percent сразу рядом, не надо считать на фронте;
  3. JSONB-сериализация массива стабильнее (объект перетасовывается).
- **Агрегаты**: медиана и p90 (R-7, как в 2.1) — переиспользованы `lib/statistics.ts`. Возвращаются `number | null` (null = пустая выборка, не 0).
- **Драфт-фильтр**: переиспользован `CycleTimeMrCalculator.isDraft(title)` — гарантия, что MR Size и Cycle Time считаются по **одному и тому же срезу** MR (важно для парной визуализации — иначе бакеты бы посчитались по одной выборке, а время фаз — по другой, и расхождение в `sampleSize` запутало бы пользователя).
- **Учёт повреждённых данных**: если `linesAdded`/`linesRemoved` пришли как `NaN`/`null` (битый sync), MR не отбрасывается, а считается с `size=0` (попадает в бакет `≤50`) — иначе занизим знаменатель и percent поедет.
- **Сервис** `metrics.service.ts:getTeamMrSize`:
  - `assertTeamAccess(actor, team)` — те же 404/403 правила, что у 2.1;
  - выборка merged-MR по проектам команды с `mergedAt ∈ [periodStart, periodEnd]` (окно по `mergedAt` для стабильности исторических замеров);
  - возвращает `TeamMrSizeReport { metricType, periodStart, periodEnd, teamUid, projectUids, value }` — `projectUids` для прозрачности расчёта (по каким проектам выборка).
- **Endpoint** `GET /api/teams/:teamUid/mr-size` зарегистрирован с `requireRole('LEAD', 'ADMIN')` — соответствие матрице ВКР 2.2.7 (LEAD команды + ADMIN для отладки). HEAD/MANAGER не имеет — MR Size это review-метрика, а не DORA, верхний уровень видит только DORA-агрегаты.
- **Обновление `MrSizeValue` в схеме** (`db/drizzle/schema/metrics/schema.ts`): добавлены `p90LinesChanged`, `sampleSize`, `excludedDrafts`; типы агрегатов `number | null` (было обязательное `number`). Поскольку `metrics_snapshots` ещё не наполняется (writer — задача 2.7), изменение JSONB-схемы безопасно — старых записей нет.
- **Smoke-проверка границ бакетов** (вручную): `50→≤50`, `51→51-200`, `200→51-200`, `201→201-400`, `400→201-400`, `401→401-800`, `800→401-800`, `801→>800` — края бакетов работают корректно (используется `size <= upper` с findIndex, верхняя граница включена).

**Осталось доработать:**

- **Снепшот в `metrics_snapshots`**: сейчас расчёт on-demand при каждом GET (как и в 2.1). Сохранение в snapshot — задача 2.7.
- **Unit-тесты** на `MrSizeCalculator.compute` (границы бакетов, percent при N=0, draft-фильтр, NaN-defensiveness) — задача 8.3.
- **Альтернативное окно по `gitlabCreatedAt`** — для дашборда «MR в работе по размеру». Не нужно для MVP.
- **Параметризация границ бакетов** на уровне проекта — за пределами MVP (хардкод из концепции CherryGit).

### 2.3. Lead Time for Changes — 🟡 15%

**Доработать:**

- Для каждого deployment найти first commit ветки: либо через `mr.firstCommitAt` (источник — `fetchMergeRequestCommits[0].authored_date`), либо через предыдущий deploy на том же ref
- `leadTime = deployedAt − firstCommitAt`
- Медиана и p90

### 2.4. Deployment Frequency — 🟡 15%

**Доработать:**

- Количество деплоев / период
- Категоризация Elite/High/Medium/Low (пороги из ВКР концепции)
- Группировка по дню/неделе/месяцу для графика

### 2.5. Change Failure Rate — 🟡 15%

**Доработать:**

- За окно после deploy (например, 7 дней) найти merged MR с hotfix-label
- `CFR = deploys_with_hotfix / total_deploys`
- Парная отрисовка с DF (обязательное требование ВКР)

### 2.6. Bus Factor — 🟡 15%

`bus-factor.calculator.ts` — 21 строка. `code-modules.service.ts` — 29 строк.

**Доработать:**

- Определение «модуля»: первая директория из пути файла (`src/auth/...` → `auth`). Источник — `mr.changes.new_path`. Хранить в таблице `code_modules` (есть в скелете).
- Расчёт: `count(distinct author_uid)` за последние 90 дней по каждому модулю
- Цветовая маркировка `<2 — red`, `2 — yellow`, `≥3 — green`

### 2.7. Snapshot-система — 🔴 5%

**Доработать:**

- `metrics_snapshots` уже в схеме, но writer отсутствует
- Cron-пересчёт после каждого sync: для каждой команды + каждой метрики + текущего периода писать snapshot
- TTL/чистка старых snapshots (или хранить навсегда для исторических графиков)

---

## 3. Ролевая модель доступа

### 3.1. Middleware и матрица — 🟢 70%

`requireRole('ADMIN')` используется. Нужно проверить полную матрицу.

**Доработать:**

- Middleware `requireTeamMembership(teamUid)` — для DEVELOPER/LEAD проверять, что состоят в команде запрашиваемых метрик
- Middleware `requireSelfOrAdmin(uid)` — для индивидуальных метрик (DEVELOPER видит ТОЛЬКО свои)
- **Тесты на 403**: индивидуальные метрики другого пользователя → 403; менеджер запрашивает индивидуалку → 403; лид запрашивает другую команду → 403

### 3.2. Фильтрация ответов по роли — 🔴 10%

**Доработать:**

- `metrics.service.ts` — единая точка: принимает `requesterUid`, `requesterRole`, `targetTeamUid`, возвращает разный набор полей
- DEVELOPER: индивидуальные значения + командный baseline
- LEAD: только агрегаты, индивидуалки скрыты
- MANAGER: только кросс-командные DORA, индивидуалки и MR-детали отсутствуют

---

## 4. Управление сущностями

### 4.1. Команды (`teams`) — 🟡 40%

`teams.service.ts` — 80 строк. Базовый CRUD скорее всего есть.

**Доработать:**

- Назначение лида команды
- Привязка участника с ролью (`DEVELOPER` | `LEAD`)
- Маппинг GitLab user → CherryGit user (нужен endpoint «привязать email или gitlab username»)
- Удаление участника из команды (с инвалидацией метрик его участия?)
- Audit для всех мутаций

### 4.2. Департаменты — 🟡 30%

`departments.service.ts` — 29 строк.

**Доработать:**

- CRUD департамента
- Привязка команд к департаменту (для скоупа MANAGER)
- Назначение руководителя департамента

### 4.3. Пользователи (ADMIN) — 🟡 50%

`users-admin.controller.ts` — 88 строк.

**Доработать:**

- Назначение/смена глобальной роли
- Деактивация пользователя
- Audit

### 4.4. Маппинг GitLab-пользователей → CherryGit-пользователей — 🔴 10%

Таблица `user_gitlab_identities` уже в схеме (`userUid`, `gitlabConnectionUid`, `gitlabUsername`, `gitlabUserId`) — резолв username работает в `sync.service.resolveAuthors` для MR/reviews.

**Доработать:**

- **Колонка `email` в `user_gitlab_identities`** — GitLab `/repository/commits` НЕ возвращает username, только `author_email`. Без email-резолва `commits.authorUid` ВСЕГДА null (Bus Factor по авторам деградирует до группировки по строке email).
- При sync `commits` сопоставлять `commit.author_email` ↔ `userGitlabIdentities.email`. Сейчас в `commits.authorGitlabUsername` пишется email как заглушка.
- Endpoint «пригласить участника по gitlab email», «связать существующего юзера с gitlab username/email» — для админа.
- Авто-резолв: при первом sync, если email совпадает с `users.email` — создавать identity автоматически.

---

## 5. Аудит — 🟢 90%

`audit.service.ts` готов, подключён в GitLab.

**Доработать:**

- Подключить `recordAuditLog` в: `projects`, `teams`, `departments`, `users-admin`, `sync` (запуск/завершение)
- Endpoint фильтра по типу события для админа (есть скелет, проверить)
- UI на фронте для просмотра журнала

---

## 6. Экспорт CSV — 🟡 20%

`export.service.ts` — 16 строк.

**Доработать:**

- Использовать готовую библиотеку (`csv-stringify`)
- Эндпоинты экспорта: метрики команды / DORA по департаменту / список MR / audit-логи
- Соблюдение ролевого скоупа в экспорте (LEAD не может экспортировать чужие команды)

---

## 7. Frontend — 🔴 0% по предметной области

Сейчас только Vite + Router + Tailwind + один example-page. **Это второй большой кусок работы наравне с sync.**

### 7.1. Инфраструктура — 🔴 0%

- Auth-страницы: login, register (нет ни одной, есть только бэкенд)
- Auth context / hook `useAuth`
- Axios interceptor: обработка 401 с auto-refresh
- Layout с боковой панелью
- Защищённые маршруты `<ProtectedRoute role="ADMIN">`
- Локализация (русский по ВКР)
- React Query (или SWR) для серверного состояния — сейчас только axios instance

### 7.2. Дашборд разработчика — 🔴 0%

- Виджеты: личный Cycle Time MR, личный MR Size, история своих метрик
- Командный baseline для сравнения
- Селектор периода

### 7.3. Дашборд тимлида — 🔴 0%

- Командные агрегаты с декомпозицией Cycle Time
- Bus Factor по модулям (heatmap)
- График распределения MR Size

### 7.4. Дашборд руководителя отдела — 🔴 0%

- DORA-метрики по командам (плитки)
- Парная отрисовка DF / CFR
- Сравнительная динамика

### 7.5. Админ-панель — 🔴 0%

- Подключения GitLab (CRUD + test)
- Список проектов и привязка к командам
- CRUD команд и департаментов
- Управление пользователями
- Журнал аудита
- Состояние sync

### 7.6. Прозрачность расчёта (требование ВКР) — 🔴 0%

- Каждая метрика — раскрывающийся блок с формулой и выборкой
- Tooltip с описанием

---

## 8. DevOps / эксплуатация

### 8.1. Docker compose end-to-end — 🟡 50%

В `CLAUDE.md` упомянуто 3 контейнера. Проверить:

- Сборка фронта в nginx-образ
- Передача env-переменных
- Healthchecks для postgres/redis
- Volume для postgres data

### 8.2. Миграции — 🟢 80%

- Drizzle настроен, миграции пишутся
- Нужна seed-миграция (минимум: один ADMIN при старте)

### 8.3. Тесты — 🔴 10%

- Jest настроен
- Тестов на новые модули нет
- **Минимально для защиты:** unit-тесты на calculators (формулы) + integration-тест на ролевой 403

### 8.4. Безопасность — 🟡 70%

- TLS на baseUrl ✅
- Шифрование PAT ✅
- Rate limiting на login ✅
- **Доработать:** rate limiting на admin endpoints (создание подключений и т.п.), CORS-whitelist, helmet headers

---

## Приоритеты по фазам

### Фаза 1 — минимум для рабочего демо (~ 45% работы)

1. ~~**Миграция: `projects.default_branch text`**~~ — ✅ внесено в схему, генерация миграции через `yarn generate`
2. ~~**Sync-сервис** (1.3)~~ — ✅ готово
3. ~~**Подключение проекта к команде** (1.2)~~ — ✅ готово
4. **Маппинг commit-author by email** (4.4) — добавить колонку `user_gitlab_identities.email` и второй резолв; без этого Bus Factor по авторам и индивидуальные метрики разработчиков не работают корректно
5. **deployment_merge_requests linking** в sync (1.3 продолжение / 2.3 prep) — связь MR с релизами, без неё Lead Time неполный
6. **Расчёт метрик** (2.1–2.6) на собранных данных — 2.1 ✅ Cycle Time MR, 2.2 ✅ MR Size; осталось 2.3–2.6
7. **Snapshot-writer** (2.7)
8. **Минимальный фронт**: login + один дашборд тимлида + админка подключений

### Фаза 2 — соответствие ролевой модели ВКР

6. Полная ролевая фильтрация (3.1, 3.2) — это «архитектурная гарантия» ВКР
7. Маппинг GitLab-юзеров (4.4)
8. Все 3 дашборда (7.2–7.4)
9. Admin-панель и журнал аудита (7.5)

### Фаза 3 — полировка под защиту

10. Прозрачность расчёта в UI (7.6)
11. Тесты на формулы + 403 (8.3)
12. Экспорт CSV (6)
13. Docker compose целиком (8.1)
14. Bus Factor + code-modules детектор (2.6)

---

## Грубая оценка готовности

| Слой                                      | Готовность                                            |
| ----------------------------------------- | ----------------------------------------------------- |
| Схема БД                                  | 95% (нужна миграция для `projects.default_branch`)    |
| Auth / User / Media (шаблон)              | 90%                                                   |
| GitLab API клиент                         | 100%                                                  |
| Подключение проектов (1.2)                | 95%                                                   |
| Sync engine (1.3)                         | 80% (нет staging, email-резолва, deploy↔MR linking)   |
| Метрики (формулы)                         | 35% (2.1 ✅ Cycle Time, 2.2 ✅ MR Size; 2.3-2.7 ⏳)   |
| Управление сущностями (teams/departments) | 30%                                                   |
| Аудит                                     | 92% (sync ✅, осталось teams/departments/users-admin) |
| GitLab user mapping (4.4)                 | 10%                                                   |
| Ролевая фильтрация                        | 20%                                                   |
| Экспорт                                   | 20%                                                   |
| Frontend (предметка)                      | 0%                                                    |
| Тесты                                     | 5%                                                    |
| Docker / deployment                       | 50%                                                   |
| **Общая по проекту**                      | **~42%**                                              |

**Бэкенд один** — около 56%. **С фронтом** — около 38–42%.

---

## 9. Аудит бэкенда (2026-05-23)

Сквозная проверка всего бэкенда против требований ВКР, концепции CherryGit и принципов «метрики измеряют процесс, не людей». Часть критических находок исправлена в этой же ревизии (отмечено ✅ FIXED), часть зафиксирована тут как доработки.

### 9.1. Критические фиксы, внесённые в этом проходе

#### 9.1.1. ✅ FIXED — Privilege escalation через `POST /api/auth/register`

**Что было:** `user.service.createUser` делал `db.insert(users).values({ ...createUserDto })`, и `role` из тела запроса записывалась как есть. Любой анонимный клиент мог отправить `{ mail, password, role: 'ADMIN' }` и сразу получить токен суперпользователя. Это сводит на нет всю ролевую модель ВКР 2.2.7 (доработка 3.x).

**Фикс** ([user.service.ts:60-63](back/src/modules/user/user.service.ts:60)): открытый `/register` принудительно сохраняет `role: 'DEVELOPER'`. Поднять роль может только админ через `/api/admin/users` (когда модуль 4.3 будет реализован).

**Защита:** в идеале `/register` вообще закрыть и пускать только через invite (отмечено в 9.2.4 ниже).

#### 9.1.2. ✅ FIXED — Mass-update паролей в `createUser`

**Что было:** после insert исполнялся `db.update(users).set({ password: ... })` БЕЗ `.where(...)` — это перезаписывало пароль ВСЕМ пользователям системы. Один успешный `/register` → пароль атакующего становится паролем всех аккаунтов, включая ADMIN.

**Фикс** ([user.service.ts:46-66](back/src/modules/user/user.service.ts:46)): мёртвый блок удалён — хешированный пароль уже корректно пишется первым insert'ом.

#### 9.1.3. ✅ FIXED — Account-takeover в `getUserByLoginData` через NULL-phone

**Что было:** `or(eq(users.mail, loginData.mail), eq(users.phone, loginData.phone))`. Если в `loginData.phone === null/undefined`, Drizzle превращает `eq(phone, null)` в SQL `phone IS NULL`, и OR-ветка цепляет ЛЮБОГО пользователя без телефона (типичный кейс — seed-админ). Дальше bcrypt сравнит присланный пароль с хешем этого админа.

**Фикс** ([user.service.ts:21-44](back/src/modules/user/user.service.ts:21)): условия добавляются в WHERE только при реально присланных значениях. Если в DTO нет ни mail, ни phone — 400 BAD_REQUEST.

#### 9.1.4. ✅ FIXED — Security headers ставились ПОСЛЕ роутера

**Что было** ([main.ts](back/src/main.ts)): порядок middleware был `app.use('/api', router); app.use(securityHeaders);`. К моменту срабатывания security-middleware response уже отправлен роутером — заголовки `X-Content-Type-Options`, `Strict-Transport-Security` и пр. фактически НЕ применялись к ответам API. Это формально нарушает доработку 8.4 «helmet headers».

**Фикс:** security middleware перенесён ПЕРЕД `cors/json/cookieParser/router`. Также убран `Feature-Policy` (заголовок устарел и заменён `Permissions-Policy`).

#### 9.1.5. ✅ FIXED — Error handlers вызывали `sendResponse` + `next()` одновременно

**Что было** ([main.ts](back/src/main.ts)): оба error-handler'а делали `next(); sendResponse(...)`. После `sendResponse` ответ отправлен; `next()` пытается передать управление следующему handler'у, тот тоже вызывает `sendResponse` → `Cannot set headers after they are sent to the client`. По факту прод-логи захламлены 500-ками поверх корректных 4xx.

**Фикс:** первый handler ловит `CustomError`-ы и отдаёт правильный statusCode; всё, что не CustomError — пробрасывается во второй (fallback 500). Никто не вызывает `next()` после `sendResponse`.

### 9.2. Системные пробелы (требуют внимания, но не блокеры MVP)

#### 9.2.1. teams/departments/users-admin сервисы — 100% `notImplemented`

Все сервисные методы возвращают 501:

- `teams.service.ts` — `createTeam/addMember/attachProject/...`
- `departments.service.ts` — `createDepartment/...`
- `users-admin.service.ts` — `listUsers/createUser/updateUser/linkGitlabIdentity/...`

**Последствие:** через REST API нельзя:

- создать команду;
- добавить участника команды и назначить LEAD'а;
- создать отдел и привязать команду;
- назначить пользователю роль LEAD/HEAD/ADMIN;
- связать пользователя с GitLab-аккаунтом.

То есть **система не вводится в эксплуатацию через UI/REST**. Сейчас единственный способ — ручные INSERT'ы в БД (или seed-скрипт). Для защиты ВКР это нормально (демо данные seed'ятся), но в тексте работы стоит честно отметить как «MVP-ограничение».

**Приоритет:** этот блок поднимет 1.2 (привязка проекта к команде) до фактической ценности — сейчас «команда» существует только как ряд в `teams`, без участников и LEAD'а, поэтому `assertTeamAccess` всегда 403 для LEAD/HEAD на боевых данных. Минимальный CRUD `teams + team_members` — следующий приоритет после 2.3-2.6.

#### 9.2.2. `assertTeamAccess` отбраковывает DEVELOPER на любом team-endpoint'е

`back/src/modules/metrics/lib/team-access.ts:80-82` — для роли DEVELOPER кидает 403 «Role has no access to team aggregates». При этом в ВКР (CLAUDE.md, дашборд разработчика): «личные метрики + командный baseline». Сейчас командный baseline для DEVELOPER через `/api/teams/:teamUid/metrics` недоступен.

**Доработать (часть 3.2):** разрешить DEVELOPER, если он состоит в команде (`team_members.userUid === actorUid`), но в этом случае сервис должен скрывать индивидуальные значения других участников.

#### 9.2.3. Эндпоинт `GET /api/teams/:teamUid/metrics` без `requireRole`

В [metrics.routes.ts:22](back/src/modules/metrics/metrics.routes.ts:22) маршрут пускает любого аутентифицированного — но `getTeamMetrics` (сервис) пока `notImplemented`. Когда метрика будет реализована — нужно явно перечислить роли: `LEAD/HEAD/ADMIN` (DEVELOPER через `/me/...`, см. 9.2.2).

#### 9.2.4. Открытый `/api/auth/register` — security risk

После фиксов 9.1.1/9.1.2 регистрация безопасна с точки зрения privilege escalation. Но архитектурно для CherryGit (внутренняя корпоративная система) **открытая регистрация неправильна**: пользователи должны создаваться админом через 4.3, либо через приглашение по invite-токену. Сейчас любой внешний клиент может зарегистрироваться как DEVELOPER и видеть `/me/metrics`. Для MVP-демо допустимо; для прод — закрыть.

**Доработать:** добавить env-флаг `ALLOW_OPEN_REGISTRATION=true`; в проде по умолчанию выключен.

#### 9.2.5. `baseSchema` использует `date()` вместо `timestamp()` для `createdAt`/`updatedAt`

[base.schema.ts:5-8](back/src/db/drizzle/schema/base.schema.ts:5) — `createdAt`/`updatedAt` объявлены как `date` (PG `DATE`). Это типы только по дате, без времени. Для audit-trail и для отладки sync'а это плохо: записи, созданные в один день, неотличимы по порядку.

Доменные timestamp'ы (`committedAt`, `mergedAt`, `gitlabCreatedAt`, `occurredAt` в `audit_logs`) — корректно объявлены как `timestamp()`, так что **метрики не страдают**. Но журнал аудита (`auditLogs.createdAt`) и любая ORDER BY `createdAt DESC` дают только подневную точность.

**Доработать:** заменить `date(...)` → `timestamp(...)` в `baseSchema`. Затронет ВСЕ таблицы — миграция требует ALTER TYPE для каждой `created_at/updated_at`.

#### 9.2.6. JWT-стор в Redis использует `KEYS *:token` (O(N))

[jwt.service.ts:48-54](back/src/modules/auth/jwt.service.ts:48), [jwt.service.ts:64-65](back/src/modules/auth/jwt.service.ts:64) — `getToken` и `removeAllTokensByUid` сканируют все ключи Redis через `KEYS`. `KEYS` блокирует Redis-инстанс на всё время скана. При 10к сессий это десятки миллисекунд блока.

**Доработать:** изменить схему хранения на `refresh:{token} → {userUid}` + индекс `user:{uid}:refresh → SET<token>`. Тогда `getToken` → `GET`, `removeAllTokensByUid` → `SMEMBERS + DEL`.

#### 9.2.7. `refresh.expiresIn` парсится как `+x.replace('h', '')`

[jwt.service.ts:43](back/src/modules/auth/jwt.service.ts:43) — `+config.jwt.refresh.expiresIn.replace('h', '')`. Если env `REFRESH_TOKEN_EXPIRES_IN=7d`, то `replace('h','') === '7d'` → `+('7d') === NaN` → `redis.SET(..., { EX: NaN })`. Поведение Redis-клиента на NaN — отбрасывает EX, ключ живёт вечно. Refresh-токены **не истекают**.

**Доработать:** парсить duration с `ms`-библиотекой или явной regex'ой `^(\d+)([smhd])$`.

#### 9.2.8. Аудит подключён только в gitlab/projects/sync

[audit.service.ts](back/src/modules/audit/audit.service.ts) реализован. `recordAuditLog` зовётся в:

- `gitlab.service.ts` — create/update/delete connection ✅
- `projects.service.ts` — connect/update/delete/resync/labels ✅
- `sync.service.ts` — completed/failed ✅

Не подключён в:

- `teams.service.ts` (пока stub — но когда оживёт, audit обязателен по требованию ВКР 2.2.3);
- `departments.service.ts`;
- `users-admin.service.ts` (особенно критично — смена роли);
- `auth.service.ts` (`login`/`logout`/`refresh` — стоит логировать `auth.failed_login` для безопасности).

#### 9.2.9. PAT-токен GitLab живёт в памяти всё время прогона `syncProject`

[sync.service.ts:106](back/src/modules/sync/sync.service.ts:106) — `decryptSecret(connection.encryptedToken)` сохраняется в замыкании `GitlabClient` на десятки минут (большой проект). Если процесс упадёт с core dump — токен утечёт.

**Доработать:** обнулять `client.token = ''` после `finally`-блока в `syncProject`. Или: расшифровывать перед каждым GitLab-запросом (overhead +5мс на 1 запрос — допустимо).

#### 9.2.10. Внутреннее имя таблицы `user_profle` (опечатка)

[user/schema.ts:31](back/src/db/drizzle/schema/user/schema.ts:31). Таблица не используется в логике (только декларация), но при выкатке миграции имя останется в БД. **Доработать:** переименовать в `user_profile` отдельной миграцией.

#### 9.2.11. `usersPhoneUnique` на nullable-колонке

[user/schema.ts:24](back/src/db/drizzle/schema/user/schema.ts:24) — `unique('users_phone_unique').on(table.phone)`. В Postgres `UNIQUE` на nullable колонке не блокирует множественные NULL (это и нужно), так что баг не возникнет, но **лучше переоформить как partial index** `CREATE UNIQUE INDEX ... WHERE phone IS NOT NULL` для явности.

#### 9.2.12. `loadOrCreateSyncStatus` — гонка при первом sync

[sync.service.ts:301-312](back/src/modules/sync/sync.service.ts:301) — при первом запуске SELECT-then-INSERT без ON CONFLICT. При параллельном вызове двух `syncProject` (planner + ручной trigger) один из них упадёт по unique constraint на `projectUid`. Обычно ловится try/catch в вызывающем коде, но лучше — `INSERT ... ON CONFLICT (project_uid) DO NOTHING RETURNING *` + повторный SELECT при пустом result.

#### 9.2.13. `N+1` в `linkDeploymentsToMergeRequests`

Уже задокументировано в разделе 1.4. Просто напоминание, что при росте >1000 деплоев на проект — резкая деградация sync-tick'а.

#### 9.2.14. Открытый Swagger UI без аутентификации

[main.ts:39](back/src/main.ts:39) — `/docs` отдаётся всем. Раскрывает структуру API (включая ADMIN-эндпоинты), что упрощает разведку для атакующего.

**Доработать:** либо вынести `/docs` за isAuthenticated + requireRole('ADMIN'), либо отдавать только в `NODE_ENV !== 'prod'`.

#### 9.2.15. Расчётные сервисы не возвращают `metricType` единообразно

В `getTeamCycleTimeMr` и `getTeamMrSize` тип возврата явный (`TeamCycleTimeMrReport`/`TeamMrSizeReport`). В будущих 2.3-2.6 нужно соблюсти ту же форму `{metricType, periodStart, periodEnd, teamUid, projectUids, value}` — иначе фронту придётся писать N разных адаптеров. Зафиксировать как соглашение в коде (общий `TeamMetricReport<T>` дженерик).

#### 9.2.16. `commits.authorGitlabUsername` хранит email вместо username

Уже задокументировано в 1.3 и 4.4. Но напоминание: пока 4.4 не сделан, **Bus Factor по авторам коммитов на боевых данных вернёт мусор** — авторы будут идентифицироваться по email-строкам, а `users_gitlab_identities.gitlab_username` от этого не зарезолвится.

### 9.3. Соответствие концепции CherryGit (контрольный лист)

| Принцип ВКР                                          | Реализован?                                                                               |
| ---------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| Метрики измеряют процесс, не людей                   | ✅ архитектурно (assertTeamAccess + role middleware на эндпоинтах)                        |
| Индивидуальные метрики приватны от руководителя      | ✅ нет endpoint'ов, отдающих чужие индивидуальные значения LEAD/HEAD                      |
| Outcome важнее activity (нет «коммитов на человека») | ✅ калькуляторы 2.1/2.2 не считают activity per-person                                    |
| Парная визуализация скорости/качества                | 🟡 эндпоинты CycleTime + MR Size отдают данные, фронт ещё рисует не парно (раздел 7)      |
| Прозрачность расчёта (формула + sampleSize)          | ✅ калькуляторы возвращают `sampleSize`, `excludedDrafts`, `projectUids` — для UI tooltip |
| HTTP 403 при попытке выйти из зоны видимости         | ✅ assertTeamAccess + requireRole                                                         |
| Шифрование PAT-токенов GitLab                        | ✅ AES-256-GCM (`lib/encryption.ts`)                                                      |
| Аудит мутаций                                        | 🟡 gitlab/projects/sync — есть; teams/departments/users-admin — отсутствует               |
| Намеренно исключённые метрики (коммиты, LoC per dev) | ✅ ни одна не реализована и нет планов                                                    |

### 9.4. Сводная переоценка готовности бэкенда после аудита

| Слой                                      | Было | Стало                                                                                |
| ----------------------------------------- | ---- | ------------------------------------------------------------------------------------ |
| Auth / User (шаблон)                      | 90%  | 78% — найдены три критических бага (✅ FIXED), но `/register` policy остался спорным |
| Безопасность (заголовки, error handling)  | 70%  | 85% — security headers и error handlers приведены в порядок                          |
| Управление сущностями (teams/departments) | 30%  | 25% — все методы остались `notImplemented`, без них система формально неюзабельна    |
| Аудит                                     | 92%  | 85% — переоценено: gitlab/projects/sync ✅, teams/departments/users-admin/auth ❌    |
| **Бэкенд (взвешенно)**                    | ~56% | **~58%** — критические фиксы security; функционально без изменений                   |

**Итог аудита:** функционал бэкенда соответствует доработкам 1.1–2.2 и 5 (audit). Архитектурные гарантии ВКР (ролевая модель, приватность индивидуальных метрик) **сохранены**. Главный недостаток — отсутствие CRUD сущностей (teams/departments/users-admin), что блокирует ввод системы в эксплуатацию без ручных INSERT'ов в БД. Этот блок должен идти ВТОРЫМ приоритетом после 2.3-2.6.
